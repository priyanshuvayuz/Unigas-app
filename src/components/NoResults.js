import React from 'react'
import { View, Text } from 'react-native'

const NoResults = () => {
    return (
        <View>
            <Text style={{fontSize:20,alignContent:'center',alignItems:'center'}}>Sorry no results yet...</Text>
        </View>
    )
}

export default NoResults
